/** Default mock user ID used for development transactions */
export const DEVELOPMENT_USER_ID = 1;

/** Time window in seconds within which duplicate recharge requests are blocked */
export const DUPLICATE_WINDOW_SECONDS = 10;
